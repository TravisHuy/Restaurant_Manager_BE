server:
  port: 8080

spring:
  data:
    mongodb:
      uri: mongodb+srv://travishuy:travishuy@travishuy.2ak1k.mongodb.net/?retryWrites=true&w=majority&appName=travishuy
      database: travishuy
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - email
              - profile
          github:
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}
            scope: user:email

jwt:
  secret: Jfz8x!A%D*G-KaPdSgVkYp3s6v9y$B?E(H+MbQeThWmZq4t7w!z%C&F)J@NcRfUjXn
  expiration: 86400000  # 24
